name: badge

on:
  workflow_run:
    workflows: [analysis]
    types:
      - completed

jobs:
  run_on_workflow_a_success:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Run on workflow A success
        run: echo "Workflow A completes successfully"

  deploy:
    runs-on: ubuntu-latest

    permissions:
      # Give the default GITHUB_TOKEN write permission to commit and push the
      # added or changed files to the repository.
      contents: write

    steps:
      - name: deplo
#      - name: 'Unzip artifact'
#        run: unzip phpinsights.zip

        uses: actions/download-artifact@v4
        with:
          name: phpinsights
          path: badges
          token: ${{ secrets.GITHUB_TOKEN }}

#      - name: Checkout repository
#        uses: actions/checkout@v3
#
#      - name: Cache Composer dependencies
#        uses: actions/cache@v3
#        with:
#          path: /tmp/composer-cache
#          key: ${{ runner.os }}-${{ hashFiles('composer.lock') }}
#
#      - name: Installing dependencies
#        uses: php-actions/composer@v6
#        with:
#          php_version: 8.2
#          php_extensions: "zip"
#
#      - name: Setup PHP
#        uses: shivammathur/setup-php@v2
#        with:
#          php-version: 8.2
#
      - name: get
        run: "wget https://img.shields.io/badge/test_badge_coverage-missing-red -O badges/coverage.svg""

      - name: upload
        # Commit all changed files back to the repository
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          file_pattern: '*.svg *.txt *.zip *.json'
          commit_message: Badges
